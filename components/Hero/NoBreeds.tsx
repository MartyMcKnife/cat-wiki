import React, { ReactElement } from "react";

export default function NoBreeds(): ReactElement {
  return (
    <p className="text-center font-medium text-2xl mt-2">
      Looks like no breeds were found!
    </p>
  );
}
